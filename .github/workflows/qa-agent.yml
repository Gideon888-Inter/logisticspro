name: QA Workflow Agent

on:
  workflow_dispatch:
    inputs:
      app_base_url:
        description: 'App URL to test'
        required: true
        default: 'https://logisticspro.pages.dev/'
        type: string
  # Enable this after your app has a stable staging/preview URL.
  # pull_request:
  #   branches: [main]

jobs:
  workflow-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Install QA agent dependencies
        working-directory: qa-agent
        run: npm install

      - name: Install Playwright browser
        working-directory: qa-agent
        run: npx playwright install --with-deps chromium

      - name: Run workflow tests
        working-directory: qa-agent
        env:
          APP_BASE_URL: ${{ inputs.app_base_url }}
          WORKFLOWS_FILE: ./workflows.smoke.json
          TEST_USER_EMAIL: ${{ secrets.TEST_USER_EMAIL }}
          TEST_USER_PASSWORD: ${{ secrets.TEST_USER_PASSWORD }}
          SKIP_AUTH_WORKFLOWS: ${{ secrets.TEST_USER_EMAIL == '' && '1' || '0' }}
        run: npm test

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: qa-agent/playwright-report/
          retention-days: 7

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: qa-agent-results
          path: qa-agent/test-results/
          retention-days: 7
