[
  {
    "name": "Public home page loads",
    "requiresAuth": false,
    "steps": [
      { "action": "goto", "path": "/" },
      { "action": "expectTitleOrUrl", "contains": "" },
      { "action": "expectVisible", "selector": "body" }
    ]
  },
  {
    "name": "App shell renders",
    "requiresAuth": false,
    "steps": [
      { "action": "goto", "path": "/" },
      { "action": "expectVisible", "selector": "body" }
    ]
  },
  {
    "name": "Authenticated dashboard smoke test",
    "requiresAuth": true,
    "steps": [
      { "action": "login" },
      { "action": "goto", "path": "/dashboard" },
      { "action": "expectVisible", "selector": "body" }
    ]
  }
]
