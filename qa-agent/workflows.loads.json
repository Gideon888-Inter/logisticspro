[
  {
    "name": "Loads page renders after login",
    "requiresAuth": true,
    "steps": [
      { "action": "login" },
      { "action": "goto", "path": "/loads" },
      { "action": "expectVisible", "selector": "body" }
    ]
  },
  {
    "name": "Loads page visible buttons do not crash",
    "requiresAuth": true,
    "steps": [
      { "action": "login" },
      { "action": "goto", "path": "/loads" },
      { "action": "expectVisible", "selector": "body" },
      { "action": "clickEveryVisible", "selector": "button", "maxClicks": 30 }
    ]
  }
]
